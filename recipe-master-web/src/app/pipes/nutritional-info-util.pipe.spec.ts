import { NutritionalInfoUtilPipe } from './nutritional-info-util.pipe';

describe('NutritionalInfoUtilPipe', () => {
  it('create an instance', () => {
    const pipe = new NutritionalInfoUtilPipe();
    expect(pipe).toBeTruthy();
  });
});
