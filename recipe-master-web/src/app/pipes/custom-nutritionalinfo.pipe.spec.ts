import { CustomNutritionalinfoPipe } from './custom-nutritionalinfo.pipe';

describe('CustomNutritionalinfoPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomNutritionalinfoPipe();
    expect(pipe).toBeTruthy();
  });
});
