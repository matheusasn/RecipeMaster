import { ProfileInfoPipe } from './profile-info.pipe';

describe('ProfileInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
