import { NutritionalInfoPercentPipe } from './nutritional-info-percent.pipe';

describe('NutritionalInfoPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new NutritionalInfoPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
